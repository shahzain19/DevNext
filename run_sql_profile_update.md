# 🖼️ NEW FEATURE: Profile Picture & Covers

To enable the new profile features (Phone, Cover Photo, Avatar Uploads), you need to run this SQL script.

## ⚡ Instructions

1.  Open **Supabase Dashboard** > **SQL Editor**
2.  Open **`ADD_PROFILE_FIELDS.sql`**
3.  Copy **ALL** the code
4.  Paste and **RUN**

## ✅ What this does
1.  **Database**: Adds `phone` and `cover_url` to your profiles.
2.  **Storage**: Creates `avatars` and `covers` buckets for file uploads.
3.  **Policies**: Allows users to upload images.

Once run, the new UI will fully work! 🚀
